#include "coweb/route.h"

namespace web {


ROUTE::ROUTE() {}

ROUTE::~ROUTE() {}
//void ROUTE::reg(fastring url, rue_callback args, ...) {
//    co::vector<rue_callback> v;
//    v = {};
//    va_list ap;
//    va_start(ap, args);
//    v.push_back(va_arg(ap, rue_callback));
//    va_end(ap);
//    rue_callback a;
//    REQ          req;
//    for (size_t i = 0; i < v.size(); i++) {
//        v[i](&req);
//    }
//}
}  // namespace web